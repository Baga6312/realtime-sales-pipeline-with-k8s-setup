apiVersion: v1
kind: Service
metadata:
  name: registry
  namespace: bigdata
spec:
  selector:
    app: registry
  ports:
    - port: 5000
      targetPort: 5000
  type: NodePort
  # NodePort so we can push from host machine